# Traffic API MVP Environment Configuration

# ============================================
# API Authentication
# ============================================

# Comma-separated list of valid API keys
# Generate secure keys with: openssl rand -hex 32
# Example: VALID_API_KEYS=key1,key2,key3
VALID_API_KEYS=demo-key-free,demo-key-pro,demo-key-enterprise

# ============================================
# Google Maps API (Fallback Traffic Data)
# ============================================

# Google Maps Directions API key for traffic fallback
# Get your API key: https://developers.google.com/maps/documentation/directions/get-api-key
# Note: This is only used when free sources (TII, WebTRIS) return no data
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ============================================
# LLM Configuration for Segment Extraction
# ============================================

# LLM Provider: 'gemini' (default), 'openai', or 'kimi'
# Gemini recommended for free tier (60 requests/min)
LLM_PROVIDER=gemini

# Your API key for the selected provider
# Get free Gemini API key: https://makersuite.google.com/app/apikey
# Get OpenAI API key: https://platform.openai.com/api-keys
# Get Kimi API key: https://platform.moonshot.cn/
LLM_API_KEY=your_api_key_here

# Optional: Specify a model (uses provider default if not set)
# Gemini models: gemini-3-flash-preview (default), gemini-3-pro-preview
# OpenAI models: gpt-4o-mini (default), gpt-4o, gpt-3.5-turbo
# Kimi models: moonshot-v1-8k (default), moonshot-v1-32k, moonshot-v1-128k
# LLM_MODEL=gemini-1.5-flash

# ============================================
# Server Configuration
# ============================================

# Port for the API server (default: 3000)
# PORT=3000

# Node environment (development/production)
# NODE_ENV=development
